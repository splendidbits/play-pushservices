# Routes
# This file defines all application routes(Higher priority routes first)
# ~~~~

# Home page
GET         /                                        @controllers.TestPushController.index()
GET         /robots.txt                              @controllers.TestPushController.robots()
